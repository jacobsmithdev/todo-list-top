export default class TodoQuery {
    constructor(query, title, description, projectId) {
        this.query = query;
        this.title = title;
        this.description = description;
        this.projectId = projectId;
    }
}